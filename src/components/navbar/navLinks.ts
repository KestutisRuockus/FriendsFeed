import { NavLinksProps } from "./types";

export const navLinks: NavLinksProps[] = [
    { name: "Home", path: "/" },
    { name: "My Profile", path: "/myprofile" },
    { name: "Log Out", path: "/" }
  ];